'use strict';

/**
 * bot-feature-setting service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::bot-feature-setting.bot-feature-setting');
