'use strict';

/**
 * discord-join-consent-record controller.
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::discord-join-consent-record.discord-join-consent-record');
