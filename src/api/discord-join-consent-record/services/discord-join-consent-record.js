'use strict';

/**
 * discord-join-consent-record service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::discord-join-consent-record.discord-join-consent-record');
