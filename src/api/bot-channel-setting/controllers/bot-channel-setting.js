'use strict';

/**
 * bot-channel-setting controller.
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::bot-channel-setting.bot-channel-setting');
