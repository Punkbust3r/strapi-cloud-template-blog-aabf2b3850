'use strict';

/**
 * bot-channel-setting service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::bot-channel-setting.bot-channel-setting');
