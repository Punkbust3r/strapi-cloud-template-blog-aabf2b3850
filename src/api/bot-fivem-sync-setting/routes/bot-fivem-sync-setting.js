'use strict';

/**
 * bot-fivem-sync-setting router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::bot-fivem-sync-setting.bot-fivem-sync-setting');
