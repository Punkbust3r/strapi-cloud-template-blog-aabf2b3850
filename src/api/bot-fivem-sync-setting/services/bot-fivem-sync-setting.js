'use strict';

/**
 * bot-fivem-sync-setting service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::bot-fivem-sync-setting.bot-fivem-sync-setting');
