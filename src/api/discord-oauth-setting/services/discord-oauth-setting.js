'use strict';

/**
 * discord-oauth-setting service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::discord-oauth-setting.discord-oauth-setting');
