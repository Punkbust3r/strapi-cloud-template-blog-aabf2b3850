'use strict';

/**
 * bot-guild-setting service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::bot-guild-setting.bot-guild-setting');
