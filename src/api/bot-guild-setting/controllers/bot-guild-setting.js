'use strict';

/**
 * bot-guild-setting controller.
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::bot-guild-setting.bot-guild-setting');
