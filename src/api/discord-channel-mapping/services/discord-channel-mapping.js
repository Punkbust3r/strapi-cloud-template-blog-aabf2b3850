'use strict';

/**
 * discord-channel-mapping service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::discord-channel-mapping.discord-channel-mapping');
