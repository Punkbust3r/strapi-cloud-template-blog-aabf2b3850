'use strict';

/**
 * linked-game-account service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::linked-game-account.linked-game-account');
